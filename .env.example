# Copy to .env and fill in. Never commit .env (already in .gitignore).
# Nothing here is required for Phase 0/1 (pure local/spec work) — filled in
# as each phase actually needs an external service.

# --- Cloud GPU / generation (Phase 2+) ---
RUNPOD_API_KEY=
FAL_KEY=
REPLICATE_API_TOKEN=
HUGGINGFACE_TOKEN=

# --- Cloud backup storage (Stage 2+, see COSTS.md) ---
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET=
CLOUDFLARE_R2_ENDPOINT=

# --- Social publishing (Phase 9) ---
INSTAGRAM_ACCESS_TOKEN=
TIKTOK_ACCESS_TOKEN=

# --- Monetization (Phase 10) ---
FANVUE_API_KEY=
